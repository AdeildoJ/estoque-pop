# Upstash Redis (Vercel Storage → Upstash Redis conecta automaticamente)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Use KV_REST_API_TOKEN para escrita (não o READ_ONLY)
# Opcional: proteger webhook do n8n
API_KEY=
